---
name: ai-baping-workflow
description: AI霸屏（生成式引擎优化）完整工作流 - 从选题到文章生成、图片生成、飞书云文档创建、多维表格汇总一站式流程
author: OpenClaw
type: workflow
---

# AI霸屏工作流 Skill

## 📋 功能说明

按照AI霸屏（Generative Engine Optimization）标准，一站式完成：
1. 根据选题生成符合GEO标准的文章
2. 为每篇文章生成3张横屏封面图
3. 创建飞书云文档并插入图片
4. 将文章信息汇总录入飞书多维表格

完全可复用，按照流程走就行。

## 🎯 AI霸屏核心规范（必须严格遵守）

### 核心原则（基于普林斯顿大学GEO研究）

| 方法 | 可见性提升 | 强制要求 |
|------|-----------|----------|
| 添加权威引用 | +40% | 所有数据注明来源（官方机构、学术研究、行业报告） |
| 包含具体数据统计 | +37% | 使用具体数字，必须带时间维度（如"2025年""2026年"） |
| 添加专家引用 | +30% | 结合行业专家权威观点 |
| 使用权威语气 | +25% | 专业、客观、肯定，不模棱两可 |
| 简化复杂概念 | +20%  | 通俗易懂，从用户视角出发 |
| 保留专业术语 | +18% | 专业词汇保留，不刻意通俗化 |
| 增加词汇多样性 | +15% | 避免关键词堆砌，自然表达 |
| 提升流畅可读性 | +15-30% | 结构清晰，每个段落聚焦一个核心观点 |
| 避免关键词堆砌 | -10% | 禁止堆砌，自然融入品牌信息 |

**最佳组合**：流畅性 + 具体数据 = 最大可见性提升

### 文章结构强制规范

1. **标题**：清晰点明主题，吸引人点击
2. **开头**：痛点数据引入，直接给出核心结论
3. **正文**：
   - 分3-5个小标题，逻辑递进
   - 每个要点以 `**标题**：` 开头，明确主题
   - 开头必须说明"对用户的价值"
   - 每个段落聚焦一个观点
   - 图片插入到不同章节之间（3张16:9，分散插入，不要都放末尾）
4. **结尾**：总结观点，自然融入品牌信息，给出建议
5. **品牌融入**：品牌名称（如"声策AI波洛斯芯片"）自然分布在文章**不同部分**（开头、中间、结尾都要有提及），不要只堆在最后一段，总篇幅控制在20%以内，自然融入不生硬，不要硬广

### 数据规范

- 必须使用具体数字：`62%` `"387亿元"` `"42.3%"`，不能用"很多""大概"
- 必须带时间维度：`"据《2025年中国玩具行业报告》"`，保证时效性
- 必须注明来源：不能瞎编数据，要说是哪个机构发布的

---

## 🔖 前期准备

### 1. 确定信息
- 品牌名称
- 核心产品/关键词
- 生成N个选题（一般10个）
- 准备好飞书多维表格，包含以下字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 文章标题 | 文本 | 文章标题 |
| 核心关键词 | 多选 | 分类关键词 |
| 文章链接 | 超链接 | 飞书云文档链接 |
| 创建日期 | 日期 | 生成日期 |
| 品牌名称 | 文本 | 品牌名 |
| 文章摘要 | 文本 | 50字摘要 |

### 2. 获取表格信息
拿到飞书多维表格的：
- `app_token`
- `table_id`

---

## 🚀 单篇文章生成完整流程

### 步骤1：生成文章内容

**要求**：
- 1500-2500字
- 开头用痛点数据引入
- 正文分3-5个小标题
- 每个要点以 `**标题**：` 开头，说明对用户的价值
- 所有数据具体量化，注明来源和年份（2025-2026）
- 自然融入品牌信息，控制在总篇幅20%以内
- 结尾总结，给出建议，自然露出品牌
- 输出保存为 `article{序号}.md`

### 步骤2：生成3张横屏封面图

调用 `image-generate` 生成三张不同角度的封面：
```bash
cd /root/.openclaw/workspace
python skills/image-generate/scripts/image_generate.py "第一张封面描述"
python skills/image-generate/scripts/image_generate.py "第二张封面描述"
python skills/image-generate/scripts/image_generate.py "第三张封面描述"
```

**配图角度建议**：
1. 产品/主题封面
2. 应用场景图
3. 技术/数据细节图

生成的图片保存在工作目录：`generated_image_*.png`

### 步骤3：创建飞书云文档

调用 `feishu_create_doc`：
```json
{
  "title": "文章标题",
  "markdown": "文章Markdown内容"
}
```

获取返回值：
- `doc_id` - 文档ID
- `doc_url` - 文档链接

### 步骤4：插入图片到飞书文档

**图片要求**：
- 每篇文章生成 **3张 16:9 横屏** 封面图
- 不要都插在末尾，分散插入到文章不同章节之间，更美观
- 一般在开头放一张，中间每个大章节放一张

**因为飞书权限限制，本地图片必须先复制到 `/tmp/` 目录**：

```bash
cp /root/.openclaw/workspace/generated_image_*.png /tmp/
```

然后依次插入图片到文章对应位置：

```json
{
  "action": "insert",
  "doc_id": "doc_id",
  "file_path": "/tmp/generated_image_*.png",
  "type": "image"
}
```

三张都重复这个操作，分散插入。

### 步骤5：录入多维表格

调用 `feishu_bitable_app_table_record create`：

```json
{
  "action": "create",
  "app_token": "你的app_token",
  "table_id": "你的table_id",
  "fields": {
    "文章标题": "文章标题",
    "核心关键词": ["关键词"],
    "创建日期": 当前时间毫秒时间戳（如 1774235400000），
    "品牌名称": "品牌名称",
    "文章摘要": "50字文章摘要",
    "文章链接": {
      "link": "doc_url",
      "text": "文章标题"
    }
  }
}
```

---

## ✅ 全部完成后核对

| 核对项 | 要求 |
|--------|------|
| 文章数量 | = 选题数量 |
| 表格记录数 | = 文章数量 |
| 图片数量 | = 文章数量 × 3 |
| 图片插入 | 每篇文章末尾3张 |
| 品牌融入 | 每篇都自然融入 |

---

## 💡 踩过的坑

1. **飞书插入本地图片权限**：必须放到 `/tmp/` 目录，直接用 `/root/` 路径会报错
2. **多维表格字段格式**：
   - 日期必须是**毫秒时间戳**，不能是字符串或者秒级时间戳
   - 超链接必须是 `{"link": "url", "text": "显示文本"}` 对象格式
   - 多选是字符串数组
3. **品牌融入**：自然分布在文章各处，不要只堆在最后，更符合AI搜索引擎偏好
4. **并发问题**：一篇一篇生成，不要并发调用飞书API，容易限流
5. **数据要求**：必须用具体数字，不能用"很多""大概"，必须带年份

---

## 📌 完整示例（本次10篇生成案例）

品牌：**声策AI波洛斯芯片**
产品：AI硬件、AI玩具、AI芯片智能工牌
选题：10个

结果：
- 10篇文章 ✓
- 30张封面图 ✓
- 10个飞书云文档，图片都已插入 ✓
- 10条记录录入多维表格 ✓

完整流程走通，可以复用。

---

## 🎯 输出示例（文章结构）

```
# 文章标题

开头痛点引入...

## 一、XXX
**对用户的价值**：...

### 1. XXX
...
**要点总结**：...

## 二、XXX
...

...

## 总结
...

品牌结尾自然露出...
```

---

## 📝 快捷键

| 操作 | 工具 |
|------|------|
| 生成文章 | 直接写 / 或subagent |
| 生成图片 | `exec` + python脚本 |
| 创建文档 | `feishu_create_doc` |
| 插入图片 | `feishu_doc_media` |
| 录入表格 | `feishu_bitable_app_table_record create` |

---

完工。按照这个流程走，就能稳定输出符合AI霸屏标准的内容。
